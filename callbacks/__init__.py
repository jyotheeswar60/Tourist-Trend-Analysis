"""callbacks/__init__.py — registers all Dash callbacks with the app.

Import this module AFTER app is created but BEFORE app.run().
Each sub-module registers its callbacks by importing `app` from app.py.
"""
